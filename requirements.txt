# Anypoint Platform Assessment - Python Dependencies
# Python-Driven Workflow Requirements

# Core dependencies for MCP integration and data processing
requests>=2.31.0
python-dateutil>=2.8.2

# Logging and utilities
colorama>=0.4.6

# Optional: Enhanced data processing
pandas>=2.0.0
numpy>=1.24.0

# Optional: Advanced text processing for semantic matching
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.21.0

# Development dependencies (optional)
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0

# Note: The MCP (Model Context Protocol) integration will be handled
# through the existing MCP server connection in the host environment.
# These scripts are designed to work with the MuleSoft MCP Server
# available at: https://www.npmjs.com/package/@mulesoft/mcp-server
